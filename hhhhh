<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HUSSEIN GAMES | Ultimate Gaming Hub</title>
    <style>
        /* --- CSS VARIABLES & RESET --- */
        :root {
            --bg-dark: #0b0e14;
            --bg-card: #151a23;
            --neon-cyan: #00f3ff;
            --neon-purple: #bc13fe;
            --text-main: #ffffff;
            --text-muted: #a0a0a0;
            --font-main: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: var(--font-main);
        }

        body {
            background-color: var(--bg-dark);
            color: var(--text-main);
            overflow-x: hidden;
        }

        /* --- HEADER --- */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 40px;
            background: rgba(11, 14, 20, 0.95);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            position: sticky;
            top: 0;
            z-index: 100;
            backdrop-filter: blur(10px);
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            letter-spacing: 2px;
            background: linear-gradient(90deg, var(--neon-cyan), var(--neon-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-transform: uppercase;
        }

        .clock {
            font-family: 'Courier New', monospace;
            color: var(--neon-cyan);
            font-weight: bold;
            border: 1px solid var(--neon-cyan);
            padding: 5px 15px;
            border-radius: 4px;
            box-shadow: 0 0 10px rgba(0, 243, 255, 0.2);
        }

        /* --- HERO SECTION --- */
        .hero {
            height: 60vh;
            background: linear-gradient(to bottom, rgba(11, 14, 20, 0.3), var(--bg-dark)), 
                        url('https://placehold.co/1920x1080/1a1f2b/FFF?text=Featured+Game+Banner');
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 20px;
        }

        .hero h1 {
            font-size: 4rem;
            text-transform: uppercase;
            text-shadow: 0 0 20px rgba(188, 19, 254, 0.6);
            margin-bottom: 20px;
        }

        .btn-primary {
            padding: 15px 40px;
            font-size: 1.2rem;
            font-weight: bold;
            color: var(--bg-dark);
            background: var(--neon-cyan);
            border: none;
            border-radius: 2px;
            cursor: pointer;
            text-transform: uppercase;
            transition: all 0.3s ease;
            box-shadow: 0 0 15px var(--neon-cyan);
        }

        .btn-primary:hover {
            background: #fff;
            box-shadow: 0 0 25px var(--neon-cyan);
            transform: scale(1.05);
        }

        /* --- GAME GRID --- */
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 50px 20px;
        }

        .section-title {
            font-size: 2rem;
            margin-bottom: 30px;
            border-left: 5px solid var(--neon-purple);
            padding-left: 15px;
        }

        .game-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .game-card {
            background: var(--bg-card);
            border-radius: 10px;
            overflow: hidden;
            position: relative;
            transition: transform 0.3s, box-shadow 0.3s;
            border: 1px solid rgba(255,255,255,0.05);
            cursor: pointer;
        }

        .game-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 0 20px rgba(188, 19, 254, 0.4);
            border-color: var(--neon-purple);
        }

        .card-img {
            width: 100%;
            height: 200px;
            background: #232a36;
            object-fit: cover;
        }

        .card-body {
            padding: 20px;
        }

        .card-title {
            font-size: 1.4rem;
            margin-bottom: 10px;
            color: white;
        }

        .card-desc {
            color: var(--text-muted);
            font-size: 0.9rem;
            margin-bottom: 20px;
        }

        .btn-play {
            display: inline-block;
            width: 100%;
            padding: 10px;
            text-align: center;
            background: transparent;
            border: 1px solid var(--neon-cyan);
            color: var(--neon-cyan);
            text-decoration: none;
            font-weight: bold;
            transition: 0.3s;
        }

        .game-card:hover .btn-play {
            background: var(--neon-cyan);
            color: var(--bg-dark);
        }

        /* Coming Soon Styles */
        .locked {
            cursor: not-allowed;
            opacity: 0.7;
        }
        .locked:hover {
            transform: none;
            box-shadow: none;
            border-color: rgba(255,255,255,0.05);
        }
        .lock-icon {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 3rem;
            color: var(--text-muted);
        }

        /* --- MODAL (GAME OVERLAY) --- */
        .modal-overlay {
            display: none; /* Hidden by default */
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            z-index: 1000;
            justify-content: center;
            align-items: center;
            backdrop-filter: blur(5px);
        }

        .modal-content {
            background: var(--bg-card);
            width: 90%;
            max-width: 800px;
            height: 90vh;
            border-radius: 10px;
            border: 2px solid var(--neon-purple);
            position: relative;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            box-shadow: 0 0 50px rgba(188, 19, 254, 0.3);
        }

        .modal-header {
            padding: 15px 20px;
            background: rgba(0,0,0,0.3);
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        .close-btn {
            background: transparent;
            border: none;
            color: #fff;
            font-size: 2rem;
            cursor: pointer;
        }

        .game-area {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            background: #0f1219;
            overflow: auto;
        }

        /* --- GAME SPECIFIC STYLES --- */
        
        /* Snake */
        canvas#snakeCanvas {
            background: #000;
            border: 2px solid var(--neon-cyan);
            box-shadow: 0 0 20px rgba(0, 243, 255, 0.2);
        }
        #snakeScore {
            position: absolute;
            top: 20px;
            left: 20px;
            color: var(--neon-cyan);
            font-size: 1.5rem;
            font-family: monospace;
        }

        /* Tic Tac Toe */
        .ttt-board {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
            width: 300px;
            height: 300px;
        }
        .ttt-cell {
            background: #1f2633;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 3rem;
            font-weight: bold;
            cursor: pointer;
            border-radius: 5px;
            transition: 0.2s;
        }
        .ttt-cell:hover { background: #2a3444; }
        .ttt-cell.x { color: var(--neon-cyan); }
        .ttt-cell.o { color: var(--neon-purple); }
        #tttStatus {
            margin-bottom: 20px;
            font-size: 1.2rem;
            color: var(--text-muted);
        }
        .ttt-controls button {
            margin-top: 20px;
            padding: 10px 20px;
            background: var(--neon-purple);
            border: none;
            color: white;
            cursor: pointer;
            border-radius: 5px;
        }

        /* Memory */
        .memory-board {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 10px;
            width: 340px;
            height: 340px;
        }
        .memory-card {
            background: var(--neon-cyan);
            border-radius: 5px;
            cursor: pointer;
            position: relative;
            transform-style: preserve-3d;
            transition: transform 0.5s;
            height: 100%;
        }
        .memory-card.flip {
            transform: rotateY(180deg);
            background: #2a3444;
        }
        .front, .back {
            width: 100%;
            height: 100%;
            position: absolute;
            backface-visibility: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 2rem;
            border-radius: 5px;
            border: 1px solid #444;
        }
        .front {
            background: #1f2633; /* Hidden side (dark) */
            transform: rotateY(180deg);
        }
        .back {
            background: linear-gradient(45deg, var(--neon-purple), #2a2a2a); /* Cover */
        }

        /* --- RESPONSIVE --- */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2.5rem; }
            .game-grid { grid-template-columns: 1fr; }
            header { padding: